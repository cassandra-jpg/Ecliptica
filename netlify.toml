[build]
  publish = "dist"
  command = "npm run build"

# Edge function that enforces authentication on ALL routes
[[edge_functions]]
  path = "/*"
  function = "auth-gate"

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
